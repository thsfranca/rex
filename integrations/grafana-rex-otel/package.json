{
  "name": "rex-otel-datasource",
  "version": "0.1.0",
  "private": true,
  "description": "Rex OpenTelemetry read API datasource for Grafana",
  "scripts": {
    "build": "echo 'Grafana plugin build deferred to operator toolchain'",
    "test": "node --test test/parse.test.js"
  }
}
