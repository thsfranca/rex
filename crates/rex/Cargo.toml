[package]
name = "rex"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[package.metadata.dist]
dist = true

[dependencies]
rex-config = { path = "../rex-config" }
rex-cli = { path = "../rex-cli" }
rex-daemon = { path = "../rex-daemon" }
rex-obs-api = { path = "../rex-obs-api" }
rex-obs-store = { path = "../rex-obs-store" }
serde_json = "1"
tempfile = "3"
tokio.workspace = true
