# Webview

This directory holds the React webview bundle that backs the REX chat side panel.

PR 1 contains only a placeholder entry (`index.tsx`) so the dual-bundle build and VSIX packaging are exercised. The chat UI, streaming renderer, and code-block actions land in PR 2.
