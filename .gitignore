# Editor / AI local config (not shared) — exception: Rex documents one shared rule under .cursor/rules/
.cursor/**/*
!.cursor/rules/
.cursor/rules/*
!.cursor/rules/documentation-content.mdc

# Temporary local-only files
TEMP_*
# PR / merge handoff (keep in GitHub UI, /tmp, or editor; not the repo)
*.pr-body.md
.pr-*.md
node_modules

# Rust (workspace build output)
/target

# Local CI log snapshots
ci-observability/
