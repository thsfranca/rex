# Editor / AI local config (not shared; keep rules in user or machine-local editor config)
.cursor/

docs/research/

# Temporary local-only files
TEMP_*
# PR / merge handoff (keep in GitHub UI, /tmp, or editor; not the repo)
*.pr-body.md
.pr-*.md
node_modules

# Inference gateway operator secrets (local REX_ROOT layout)
gateway/.env

# Rust (workspace build output)
/target

# Local CI log snapshots
ci-observability/
