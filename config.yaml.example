# Rex config — entirely optional.
# Copy this file to ~/.rex/config.yaml to use it.
# Rex discovers models automatically from environment variables and local runtimes.
# Use this file only to override defaults or add models Rex can't discover.

# server:
#   host: "0.0.0.0"
#   port: 9000

# providers:
#   - prefix: "anthropic"
#     api_base: "https://litellm-proxy.company.com/anthropic"
#     api_key_env: "ANTHROPIC_AUTH_TOKEN"
#   - prefix: "openai"
#     api_base_env: "OPENAI_PROXY_URL"
#     api_key: "sk-..."

# models:
#   - name: "my-company/custom-model"
#     api_base: "https://internal.company.com/v1"
#     api_key: "sk-..."

# routing:
#   primary_model: "ollama/llama3"

# enrichments:
#   task_decomposition: true

# llm_judge:
#   enabled: true
#   model: "ollama/llama3"
#   confidence_threshold: 0.5

# learning:
#   db_path: "~/.rex/decisions.db"
#   embeddings_model: "all-MiniLM-L6-v2"
